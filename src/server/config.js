export const host = 'www.piscium.site'
//export const host = 'localhost'
export const webPort = 80
export const apiPort = 3000
export const site = 'llk/api'
export const apiHost = `http://${host}:${apiPort}/${site}`